int get_even_numbers(int x) {
    return (x ~! 2) == 0;
}

int main() {

    float 1_j;
    int i^;
    int list L;
    
    j = .5;

    for(i = 0; i < 10; i++) {
        int number;
        write("Enter a number: ");
        read(number);
        L = number : L;
    }

    int list AUXL;

    AUXL = get_even_numbers << L;

    int list AUXL;
    int list IT;

    for (IT=NEW_LIST; IT != NIL; IT = !IT) {
        write(IT[0]); 
        write(" ");
    }

    return 0;
}