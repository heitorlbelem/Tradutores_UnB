int main() {
    int 549a;
    float 1$bas;
    int i#a;
    1/b;
    float i_\b1;
    i = i_\b1;
    read(a);
    float ~b;
    read(b);

    if(a > 1b) {
        write("a > b");
    } else if(a < b) {
        write("a < b");
    } else {
        write("a = b");
    }

    return 0;
}